import { emitTrackingEvents } from './eventing';
import { emitTrackingEventsQueue } from './queue';

console.log('--- QUEUE EXAMPLE ---');

// emitTrackingEvents();
emitTrackingEventsQueue();

// emit tracking event 
// emit tracking event 6
// emit tracking event with await
// emit it using the error

// emit it using queue without error
// emit event 6
// emit with error